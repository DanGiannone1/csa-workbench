# CSA Workbench design system

`src/tokens.css` is the production visual source. The warm light palette is intentional; dark mode
and palette selection do not ship. `reference/claude-design/` is an attributable read-only import
from commit `eb0708b`; `.dc.html`, generated `support.js`, and prototype CSS are never bundled.
